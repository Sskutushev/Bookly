📚 BOOKLY — Telegram Mini App для онлайн-библиотеки
Детальный план реализации

🎨 ДИЗАЙН-СИСТЕМА
Цветовая палитра
Светлая тема:

Primary: #8B7FF5 (мягкий фиолетовый)
Secondary: #FF9B9B (персиковый)
Accent: #FFE45E (солнечный желтый)
Background: #F8F9FE (светло-серый с голубым оттенком)
Cards: #FFFFFF с легкой тенью
Text Primary: #1A1A2E
Text Secondary: #6B7280

Темная тема:

Primary: #9B8AFF (светло-фиолетовый)
Secondary: #FF6B9D (розовый)
Accent: #FFD93D (золотистый)
Background: #0F0F1E (глубокий темно-синий)
Cards: #1A1A2E с неоновым свечением
Text Primary: #FFFFFF
Text Secondary: #9CA3AF

Визуальные эффекты
Градиенты:
css/_ Hero градиент _/
background: linear-gradient(135deg, #8B7FF5 0%, #FF9B9B 100%);

/_ Карточка книги (hover) _/
background: linear-gradient(180deg, transparent 0%, rgba(139,127,245,0.1) 100%);

/_ Неоновое свечение (темная тема) _/
box-shadow: 0 0 20px rgba(155,138,255,0.3);
Анимации:

Плавное появление карточек: opacity + translateY (stagger по 0.1s)
Hover эффект книг: scale(1.05) + shadow увеличение
Skeleton loading для обложек
Ripple эффект на кнопках

Bento-компоненты
Размеры карточек:

Desktop: Book card = 220px × 320px
Mobile: Book card = 140px × 200px
Popup Desktop: max-width: 1000px
Popup Mobile: max-width: 350px

Скругления:
cssborder-radius: 24px; /_ Основные карточки _/
border-radius: 16px; /_ Кнопки и инпуты _/
border-radius: 12px; /_ Мелкие элементы _/

```

---

## 🏗️ АРХИТЕКТУРА ПРОЕКТА
```

bookly/
├── frontend/ # React + TypeScript + Vite
│ ├── src/
│ │ ├── app/ # App providers, routing
│ │ ├── pages/ # Page components
│ │ │ ├── HomePage/
│ │ │ ├── FavoritesPage/
│ │ │ ├── MyBooksPage/
│ │ │ └── ProfilePage/
│ │ ├── widgets/ # Complex UI blocks
│ │ │ ├── BookGrid/
│ │ │ ├── BookModal/
│ │ │ └── ProfileSidebar/
│ │ ├── features/ # Business features
│ │ │ ├── auth/
│ │ │ ├── book-reader/
│ │ │ ├── payment/
│ │ │ └── favorites/
│ │ ├── entities/ # Business entities
│ │ │ ├── book/
│ │ │ └── user/
│ │ ├── shared/ # Shared UI & utils
│ │ │ ├── ui/
│ │ │ ├── api/
│ │ │ ├── lib/
│ │ │ └── config/
│ │ └── main.tsx
│ ├── tailwind.config.js
│ └── vite.config.ts
│
├── backend/ # Node.js + Express + Prisma
│ ├── src/
│ │ ├── routes/
│ │ │ ├── auth.routes.ts
│ │ │ ├── books.routes.ts
│ │ │ ├── payment.routes.ts
│ │ │ └── user.routes.ts
│ │ ├── controllers/
│ │ ├── services/
│ │ ├── middleware/
│ │ ├── prisma/
│ │ │ └── schema.prisma
│ │ └── server.ts
│ └── package.json
│
└── telegram-bot/ # Python + Telegram Bot API
├── bot.py
├── handlers/
├── notifications.py
└── requirements.txt

```

---

## 📱 СТРУКТУРА ЭКРАНОВ

### 1. ГЛАВНАЯ СТРАНИЦА (Home)

**Компоненты:**

**Header** (fixed, backdrop-blur)
```

┌─────────────────────────────────────────┐
│ [Logo] [Search 🔍] [👤/Войти] │
└─────────────────────────────────────────┘

```
- Logo: Текст "Bookly" + иконка книги
- Search: Input с иконкой лупы, placeholder "Найти книгу..."
- Авторизация:
  - Не авторизован: кнопка "Войти"
  - Авторизован: Аватар (круг 40px) или первая буква имени

**Фильтры жанров** (горизонтальный скролл)
```

[ Все ] [ Детектив ] [ Романтика ] [ Фантастика ] ...

```
- Pills с мягкими скруглениями
- Активный жанр: gradient background
- Hover: subtle scale

**Bento Grid витрина книг**
```

Desktop (3 колонки):
┌───────┐ ┌───────┐ ┌───────┐
│ Book │ │ Book │ │ Book │
│ Cover │ │ Cover │ │ Cover │
└───────┘ └───────┘ └───────┘

Mobile (2 колонки):
┌─────┐ ┌─────┐
│ Book│ │ Book│
└─────┘ └─────┘

```

**Карточка книги:**
- Обложка (object-fit: cover)
- Hover: легкий подъем + shadow
- Badge "Бесплатно" / "149₽" (верхний правый угол)
- Иконка ❤️ (избранное, правый верхний)

**Bottom Navigation** (mobile only, fixed)
```

┌───────┬───────┬───────┬───────┐
│ 🏠 │ ⭐ │ 📚 │ 👤 │
│Главная│Избран.│Мои │Профиль│
└───────┴───────┴───────┴───────┘

```

---

### 2. МОДАЛЬНОЕ ОКНО КНИГИ

**Desktop (1000px width):**
```

┌──────────────────────────────────────────────┐
│ [×] │
│ │
│ ┌────────┐ Полное название книги │
│ │ │ Автор: Имя Автора │
│ │ Cover │ │
│ │ │ О книге: │
│ │ 220× │ Краткое описание книги в 2-3 │
│ │ 320px │ строки. Lorem ipsum dolor sit... │
│ └────────┘ │
│ Жанры: │
│ [Детектив] [Триллер] [Драма] │
│ │
│ ┌──────────────┐ ┌────────────┐ │
│ │ 📖 Фрагмент │ │ 📚 Читать │ │
│ └──────────────┘ └────────────┘ │
└──────────────────────────────────────────────┘

```

**Mobile (350px width):**
```

┌──────────────────────────┐
│ [×] │
│ │
│ ┌────────┐ │
│ │ │ │
│ │ Cover │ │
│ │ 140× │ │
│ │ 200px │ │
│ └────────┘ │
│ │
│ Название книги │
│ Автор │
│ │
│ О книге: │
│ Текст описания... │
│ │
│ [Жанр1] [Жанр2] │
│ │
│ ┌────────────────────┐ │
│ │ 📖 Фрагмент │ │
│ └────────────────────┘ │
│ ┌────────────────────┐ │
│ │ 📚 Читать │ │
│ └────────────────────┘ │
└──────────────────────────┘

```

**Логика взаимодействия:**

**Кнопка "Читать фрагмент":**
1. Открывает reader view (новый экран)
2. Показывает первые 10-15 страниц
3. В конце фрагмента: "Купить полную версию" или "Читать далее" (если бесплатная)

**Кнопка "Читать":**

**Если книга бесплатная:**
1. Автоматически добавляется в "Мои книги"
2. Открывается reader view
3. Toast: "Книга добавлена в вашу библиотеку"

**Если книга платная:**
1. Проверка: куплена ли книга?
   - Да → открыть reader
   - Нет → открыть Payment Modal

---

### 3. МОДАЛЬНОЕ ОКНО ОПЛАТЫ
```

┌──────────────────────────────────────┐
│ Оформление покупки │
│ │
│ ┌────┐ Название книги │
│ │Обл.│ Автор │
│ └────┘ Цена: 299₽ │
│ │
│ Выберите способ оплаты: │
│ │
│ ○ ЮKassa (карта, СБП, Apple Pay) │
│ ○ USDT (TON Network) │
│ ○ USDT (TRC20) │
│ │
│ ┌──────────────────────────────┐ │
│ │ Оплатить 299₽ │ │
│ └──────────────────────────────┘ │
└──────────────────────────────────────┘

```

**Процесс оплаты:**

**ЮKassa:**
1. Редирект на форму ЮKassa
2. После успешной оплаты → webhook на бэкенд
3. Добавление книги в purchased_books
4. Редирект обратно в приложение с toast "Книга куплена!"

**USDT (TON):**
1. Генерация уникального адреса кошелька
2. QR-код + адрес для копирования
3. "Сумма: 5 USDT (≈ 299₽)"
4. Кнопка "Я оплатил"
5. Backend слушает blockchain → подтверждает транзакцию
6. Книга добавляется в purchased_books

**USDT (TRC20):**
- Аналогично TON, но другая сеть

---

### 4. READER VIEW (Чтение книги)
```

Desktop:
┌──────────────────────────────────────────┐
│ [← Назад] Название книги [☰] │
├──────────────────────────────────────────┤
│ │
│ Текст книги, страница 1 │
│ │
│ Lorem ipsum dolor sit amet, │
│ consectetur adipiscing elit... │
│ │
│ │
├──────────────────────────────────────────┤
│ [←] Страница 1 из 245 [→] │
│ ═════════════════════════════ │
└──────────────────────────────────────────┘

Mobile: аналогично, адаптивная типографика

```

**Функционал:**
- Листание: свайп влево/вправо (mobile), кнопки (desktop)
- Прогресс: полоска внизу (заполнена = прочитано)
- Настройки (☰):
  - Размер шрифта: S / M / L / XL
  - Шрифт: Serif / Sans / Mono
  - Тема: Light / Dark / Sepia
  - Яркость: slider
- Автосохранение позиции чтения
- Bookmark (закладка): долгий тап на странице

---

### 5. ИЗБРАННОЕ (Favorites)
```

┌─────────────────────────────────────┐
│ ⭐ Избранное │
├─────────────────────────────────────┤
│ │
│ [Фильтр: Все ▾] [Сортировка ▾] │
│ │
│ Bento Grid (аналогично главной) │
│ ┌─────┐ ┌─────┐ ┌─────┐ │
│ │Book │ │Book │ │Book │ │
│ └─────┘ └─────┘ └─────┘ │
│ │
│ Если пусто: │
│ ┌────────────────────┐ │
│ │ 📚 │ │
│ │ Здесь пока пусто │ │
│ │ Добавляйте книги │ │
│ │ в избранное │ │
│ └────────────────────┘ │
└─────────────────────────────────────┘

```

**Логика:**
- Нажатие ❤️ на карточке → добавить/удалить из избранного
- Синхронизация с бэкендом (debounce 500ms)
- Фильтры: по жанру, по статусу (прочитано/не прочитано)
- Сортировка: по дате добавления, по названию, по автору

---

### 6. МОИ КНИГИ (My Books)
```

┌─────────────────────────────────────┐
│ 📚 Мои книги │
├─────────────────────────────────────┤
│ │
│ [Все] [Читаю] [Прочитано] [Куплено]│
│ │
│ Bento Grid с progress indicators: │
│ ┌─────┐ ┌─────┐ ┌─────┐ │
│ │Book │ │Book │ │Book │ │
│ │ │ │ │ │ │ │
│ │●●●○○│ │●●●●●│ │●○○○○│ │
│ │ 60% │ │100% │ │ 20% │ │
│ └─────┘ └─────┘ └─────┘ │
│ │
└─────────────────────────────────────┘

```

**Статусы:**
- **Читаю**: progress > 0% и < 100%
- **Прочитано**: progress = 100%
- **Куплено**: все купленные книги

**Дополнительно:**
- Long press → контекстное меню: "Удалить", "В избранное"
- Клик → открыть на последней прочитанной странице

---

### 7. ПРОФИЛЬ (Profile)
```

Desktop (sidebar + content area):
┌─────────────────────────────────────────────────┐
│ Sidebar (300px) │ Content Area │
│ │ │
│ ┌─────────────────┐ │ ┌──────────────────┐ │
│ │ ┌────┐ │ │ │ Мои покупки │ │
│ │ │Ава │ Татьяна│ │ │ │ │
│ │ └────┘ │ │ │ ┌──────────────┐ │ │
│ │ │ │ │ │📕 Книга 1 │ │ │
│ │ ┌─────────────┐│ │ │ │ 299₽ │ │ │
│ │ │Мои покупки ││ │ │ └──────────────┘ │ │
│ │ └─────────────┘│ │ │ │ │
│ │ ┌─────────────┐│ │ │ ┌──────────────┐ │ │
│ │ │Вход и ││ │ │ │📘 Книга 2 │ │ │
│ │ │безопасность ││ │ │ │ 199₽ │ │ │
│ │ └─────────────┘│ │ │ └──────────────┘ │ │
│ │ ┌─────────────┐│ │ └──────────────────┘ │
│ │ │Уведомления ││ │ │
│ │ └─────────────┘│ │ │
│ │ ┌─────────────┐│ │ │
│ │ │Помощь ││ │ │
│ │ └─────────────┘│ │ │
│ │ │ │ │
│ │ ┌─────────────┐│ │ │
│ │ │🚪 Выйти ││ │ │
│ │ └─────────────┘│ │ │
│ └─────────────────┘ │ │
└──────────────────────────────────────────────────┘

Mobile (tabs):
┌──────────────────────────────┐
│ ┌────┐ Татьяна │
│ │Ава │ tatyana@mail.ru │
│ └────┘ [Редактировать] │
├──────────────────────────────┤
│ ┌─────────────────────────┐│
│ │ 📦 Мои покупки ││
│ └─────────────────────────┘│
│ ┌─────────────────────────┐│
│ │ 🔐 Вход и безопасность ││
│ └─────────────────────────┘│
│ ┌─────────────────────────┐│
│ │ 🔔 Уведомления ││
│ └─────────────────────────┘│
│ ┌─────────────────────────┐│
│ │ ❓ Помощь ││
│ └─────────────────────────┘│
│ ┌─────────────────────────┐│
│ │ 🚪 Выйти ││
│ └─────────────────────────┘│
└──────────────────────────────┘

```

#### 7.1. Мои покупки
```

┌────────────────────────────────────┐
│ История покупок │
├────────────────────────────────────┤
│ ┌──────────────────────────────┐ │
│ │ 📕 Название книги │ │
│ │ Автор │ │
│ │ 299₽ • 12.01.2025 │ │
│ └──────────────────────────────┘ │
│ │
│ ┌──────────────────────────────┐ │
│ │ 📘 Другая книга │ │
│ │ Другой автор │ │
│ │ 199₽ • 10.01.2025 │ │
│ └──────────────────────────────┘ │
│ │
│ [Показать еще] │
└────────────────────────────────────┘

```

**Данные:**
- Название книги (кликабельно → открыть книгу)
- Автор
- Цена
- Дата покупки
- Способ оплаты (иконка)

#### 7.2. Вход и безопасность
```

┌────────────────────────────────────┐
│ Вход и безопасность │
├────────────────────────────────────┤
│ │
│ Email │
│ tatyana@mail.ru │
│ [Изменить email] │
│ │
│ Пароль │
│ •••••••• │
│ [Изменить пароль] │
│ │
│ Двухфакторная аутентификация │
│ ○ Выключена │
│ [Включить 2FA] │
│ │
│ Привязанные аккаунты │
│ ✓ Telegram: @tatyana │
│ [Отвязать] │
│ │
└────────────────────────────────────┘

```

**Модальные окна:**

**Изменить email:**
```

┌───────────────────────────┐
│ Новый email: │
│ [_____________________] │
│ │
│ Пароль для подтверждения: │
│ [_____________________] │
│ │
│ [Отмена] [Сохранить] │
└───────────────────────────┘

```

**Изменить пароль:**
```

┌───────────────────────────┐
│ Текущий пароль: │
│ [_____________________] │
│ │
│ Новый пароль: │
│ [_____________________] │
│ │
│ Повторите пароль: │
│ [_____________________] │
│ │
│ [Отмена] [Изменить] │
└───────────────────────────┘

```

**Включить 2FA:**
```

┌────────────────────────────┐
│ 1. Установите Google │
│ Authenticator │
│ │
│ 2. Отсканируйте QR-код: │
│ ┌──────────┐ │
│ │ QR CODE │ │
│ └──────────┘ │
│ │
│ 3. Введите код из приложения│
│ [______] │
│ │
│ [Отмена] [Подтвердить] │
└────────────────────────────┘

```

#### 7.3. Уведомления
```

┌─────────────────────────────────────┐
│ Настройки уведомлений │
├─────────────────────────────────────┤
│ │
│ ☑ Новые книги в избранных жанрах │
│ ☑ Напоминание о недочитанных книгах│
│ ☑ Специальные предложения │
│ ☐ Новости и обновления │
│ │
│ ───────────────────────────── │
│ │
│ Частота напоминаний: │
│ ○ Каждый день │
│ ● Раз в 3 дня │
│ ○ Раз в неделю │
│ │
│ ───────────────────────────── │
│ │
│ Telegram-уведомления: │
│ ☑ Включены │
│ Бот: @bookly_notifications_bot │
│ │
└─────────────────────────────────────┘

```

**Типы уведомлений в Telegram:**
1. **"Давно не заходили"**: "Привет! Мы скучали по тебе. В библиотеке появилось 10 новых книг в жанре [любимый жанр]"
2. **"Незаконченная книга"**: "Вы остановились на 73% книги '[Название]'. Продолжить чтение?"
3. **"Новинка в жанре"**: "Новая книга в жанре Детектив: '[Название]' уже доступна!"
4. **"Скидка на книгу"**: "Специальное предложение: книга '[Название]' со скидкой 30%!"

#### 7.4. Помощь

**Bento-карточки с информацией:**
```

┌─────────────────────────────────────┐
│ ❓ Помощь │
├─────────────────────────────────────┤
│ │
│ ┌────────────────────────────────┐│
│ │ 🚀 Как начать ││
│ │ Краткое руководство по ││
│ │ использованию приложения ││
│ └────────────────────────────────┘│
│ │
│ ┌────────────────────────────────┐│
│ │ 💳 Способы оплаты ││
│ │ ЮKassa, USDT TON, USDT TRC20 ││
│ └────────────────────────────────┘│
│ │
│ ┌────────────────────────────────┐│
│ │ 📖 Как читать книги ││
│ │ Reader, закладки, настройки ││
│ └────────────────────────────────┘│
│ │
│ ┌────────────────────────────────┐│
│ │ ❤️ Избранное и коллекции ││
│ │ Организация личной библиотеки ││
│ └────────────────────────────────┘│
│ │
│ ┌────────────────────────────────┐│
│ │ 🔐 Безопасность ││
│ │ 2FA, смена пароля, приватность ││
│ └────────────────────────────────┘│
│ │
│ ┌────────────────────────────────┐│
│ │ 📧 Связь с поддержкой ││
│ │ support@bookly.app ││
│ │ Telegram: @bookly_support ││
│ └────────────────────────────────┘│
│ │
└─────────────────────────────────────┘

```

**При клике на карточку → раскрывается детальная информация (Accordion)**

---

### 8. АУТЕНТИФИКАЦИЯ

#### Модальное окно входа
```

┌──────────────────────────────────┐
│ Вход в аккаунт │
├──────────────────────────────────┤
│ │
│ Email: │
│ [_________________________] │
│ │
│ Пароль: │
│ [_________________________] │
│ │
│ [Забыли пароль?] │
│ │
│ ┌───────────────────────────┐ │
│ │ Войти │ │
│ └───────────────────────────┘ │
│ │
│ ──────── или ──────── │
│ │
│ ┌───────────────────────────┐ │
│ │ 🔵 Войти через Telegram │ │
│ └───────────────────────────┘ │
│ │
│ Нет аккаунта? [Регистрация] │
│ │
└──────────────────────────────────┘

```

#### Модальное окно регистрации
```

┌──────────────────────────────────┐
│ Регистрация │
├──────────────────────────────────┤
│ │
│ Имя: │
│ [_________________________] │
│ │
│ Email: │
│ [_________________________] │
│ │
│ Пароль: │
│ [_________________________] │
│ │
│ Повторите пароль: │
│ [_________________________] │
│ │
│ ☑ Я согласен с условиями │
│ использования │
│ │
│ ┌───────────────────────────┐ │
│ │ Зарегистрироваться │ │
│ └───────────────────────────┘ │
│ │
│ ──────── или ──────── │
│ │
│ ┌───────────────────────────┐ │
│ │ 🔵 Регистрация через TG │ │
│ └───────────────────────────┘ │
│ │
│ Уже есть аккаунт? [Войти] │
│ │
└──────────────────────────────────┘
Логика Telegram Auth:

Клик "Войти через Telegram"
Редирект на Telegram Web App → авторизация 3. Получение telegram_id, first_name, username 4. Backend проверяет: есть ли пользователь с таким telegram_id?

Да → вход
Нет → создание нового аккаунта (email = telegram_id@bookly.internal)

JWT токен → сохранение в localStorage
Редирект на главную

🔧 ТЕХНИЧЕСКИЙ СТЕК
Frontend
Core:

React 18.3
TypeScript 5.3
Vite 5.0
Tailwind CSS 3.4.0

State Management:

Zustand (для глобального состояния: user, cart)
TanStack Query (для server state)

UI Libraries:

Headless UI (модалки, transitions)
React Hot Toast (уведомления)
Framer Motion (анимации)
Swiper (для листания книг)

Forms:

React Hook Form + Zod validation

API:

Axios (с interceptors для JWT)

Backend
Core:

Node.js 20.x
Express.js
Prisma ORM
PostgreSQL

Auth:

JWT (access + refresh tokens)
bcrypt (хеширование паролей)
speakeasy (2FA TOTP)

Payment:

ЮKassa SDK
TON Connect (USDT TON)
TronWeb (USDT TRC20)

File Storage:

AWS S3 / Cloudflare R2 (обложки книг)
PDF.js (для рендеринга книг)

Telegram Bot
Core:

Python 3.11
python-telegram-bot
APScheduler (для уведомлений)

Integration:

Redis (для очереди уведомлений)
PostgreSQL (shared с backend)

💾 БАЗА ДАННЫХ (Prisma Schema)
prisma// schema.prisma

model User {
id String @id @default(uuid())
email String @unique
password String? // null для Telegram auth
name String
avatar String?
telegram_id String? @unique
telegram_username String?
twoFactorSecret String?
createdAt DateTime @default(now())

favorites Favorite[]
purchases Purchase[]
readingProgress ReadingProgress[]
notifications NotificationSettings?
}

model Book {
id String @id @default(uuid())
title String
author String
description String
coverUrl String
pdfUrl String
price Float // 0 = бесплатная
genres Genre[]
isFree Boolean @default(false)
pageCount Int
createdAt DateTime @default(now())

favorites Favorite[]
purchases Purchase[]
readingProgress ReadingProgress[]
}

model Genre {
id String @id @default(uuid())
name String @unique
books Book[]
}

model Favorite {
id String @id @default(uuid())
userId String
bookId String
createdAt DateTime @default(now())

user User @relation(fields: [userId], references: [id])
book Book @relation(fields: [bookId], references: [id])

@@unique([userId, bookId])
}

model Purchase {
id String @id @default(uuid())
userId String
bookId String
amount Float
paymentMethod String // "yookassa" | "usdt_ton" | "usdt_trc20"
status String // "pending" | "completed" | "failed"
transactionId String?
createdAt DateTime @default(now())

user User @relation(fields: [userId], references: [id])
book Book @relation(fields: [bookId], references: [id])
}

model ReadingProgress {
id String @id @default(uuid())
userId String
bookId String
currentPage Int @default(1)
progress Float @default(0) // 0-100%
lastReadAt DateTime @default(now())

user User @relation(fields: [userId], references: [id])
book Book @relation(fields: [bookId], references: [id])

@@unique([userId, bookId])
}

model NotificationSettings {
id String @id @default(uuid())
userId String @unique
newBooksInGenre Boolean @default(true)
unfinishedReminder Boolean @default(true)
specialOffers Boolean @default(true)
newsAndUpdates Boolean @default(false)
frequency String @default("3days") // "daily" | "3days" | "weekly"
telegramEnabled Boolean @default(true)

user User @relation(fields: [userId], references: [id])
}

```

---

## 🔄 API ENDPOINTS

### Auth
```

POST /api/auth/register # Регистрация (email/password)
POST /api/auth/login # Вход
POST /api/auth/telegram # Telegram OAuth
POST /api/auth/refresh # Обновление access token
POST /api/auth/logout # Выход
POST /api/auth/forgot-password # Восстановление пароля
POST /api/auth/reset-password # Сброс пароля
POST /api/auth/2fa/setup # Настройка 2FA
POST /api/auth/2fa/verify # Проверка 2FA кода

```

### Books
```

GET /api/books # Список книг (+ фильтры, пагинация)
GET /api/books/:id # Одна книга
GET /api/books/:id/fragment # Фрагмент книги (первые 15 страниц)
GET /api/books/genres # Список жанров

```

### User
```

GET /api/user/profile # Профиль пользователя
PATCH /api/user/profile # Обновление профиля
POST /api/user/avatar # Загрузка аватара
PATCH /api/user/password # Смена пароля
PATCH /api/user/email # Смена email

```

### Favorites
```

GET /api/favorites # Список избранного
POST /api/favorites/:bookId # Добавить в избранное
DELETE /api/favorites/:bookId # Удалить из избранного

```

### My Books
```

GET /api/my-books # Купленные/начатые книги
GET /api/my-books/:bookId/read # Чтение книги (полный PDF)

```

### Reading Progress
```

GET /api/progress/:bookId # Прогресс по книге
POST /api/progress/:bookId # Обновить прогресс

```

### Payment
```

POST /api/payment/create # Создать платеж
POST /api/payment/yookassa # ЮKassa webhook
POST /api/payment/usdt/ton # Генерация TON адреса
POST /api/payment/usdt/trc20 # Генерация TRC20 адреса
POST /api/payment/verify # Проверка крипто-транзакции

```

### Notifications
```

GET /api/notifications/settings # Настройки уведомлений
PATCH /api/notifications/settings # Обновить настройки

```

---

## 🤖 TELEGRAM BOT

### Функционал

**Команды:**
```

/start - Приветствие + кнопка открыть Mini App
/library - Открыть Mini App на странице "Мои книги"
/help - Помощь
Уведомления (через APScheduler):

1. "Давно не заходили" (каждые 3 дня, если user неактивен):
   pythonasync def send_inactive_reminder(user_id: int):
   text = (
   "📚 Привет! Мы скучали по тебе.\n\n"
   "В библиотеке появилось 10 новых книг "
   "в жанре Детектив. Загляни!"
   )
   keyboard = [
   [InlineKeyboardButton("Открыть библиотеку", web_app=WebAppInfo(url=MINI_APP_URL))]
   ]
   await bot.send_message(user_id, text, reply_markup=InlineKeyboardMarkup(keyboard))
2. "Незаконченная книга" (раз в 7 дней):
   pythonasync def send_unfinished_reminder(user_id: int, book_title: str, progress: int):
   text = (
   f"📖 Вы остановились на {progress}% книги\n"
   f"'{book_title}'.\n\n"
   f"Продолжить чтение?"
   ) # + кнопка с deep link на книгу
3. "Новинка в жанре":
   pythonasync def send_new_book_in_genre(user_id: int, book: Book, genre: str):
   text = (
   f"🆕 Новая книга в жанре {genre}!\n\n"
   f"📕 {book.title}\n"
   f"✍️ {book.author}\n\n"
   f"{'Бесплатно!' if book.isFree else f'Цена: {book.price}₽'}"
   )

🎨 АДАПТИВНОСТЬ И КРОССБРАУЗЕРНОСТЬ
Breakpoints (Tailwind)
js// tailwind.config.js
module.exports = {
theme: {
screens: {
'sm': '640px', // Mobile landscape
'md': '768px', // Tablet
'lg': '1024px', // Desktop
'xl': '1280px', // Large desktop
'2xl': '1536px', // Extra large
}
}
}
Responsive Grid
tsx// Desktop: 3 колонки
// Tablet: 2 колонки
// Mobile: 2 колонки (узкие)

<div className="grid grid-cols-2 md:grid-cols-2 lg:grid-cols-3 gap-4">
  {books.map(book => <BookCard key={book.id} book={book} />)}
</div>
Browser Support
json// package.json
{
  "browserslist": [
    "> 0.5%",
    "last 2 versions",
    "not dead",
    "iOS >= 12",
    "Android >= 5"
  ]
}
```

### Полифиллы

- Core-js (для старых браузеров)
- Intersection Observer polyfill (для lazy loading)

---

## 🚀 ДЕПЛОЙ НА VERCEL

### Структура репозитория

```
bookly/
├── frontend/          # React app
│   └── vercel.json   # Vercel config для фронта
├── backend/          # Express API
│   └── vercel.json   # Vercel config для API
└── telegram-bot/     # Отдельный сервер (не на Vercel)
vercel.json (Frontend)
json{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Frame-Options", "value": "ALLOW-FROM https://web.telegram.org" }
      ]
    }
  ]
}
vercel.json (Backend)
json{
  "version": 2,
  "builds": [
    { "src": "src/server.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/server.ts" }
  ],
  "env": {
    "DATABASE_URL": "@database_url",
    "JWT_SECRET": "@jwt_secret",
    "YOOKASSA_SHOP_ID": "@yookassa_shop_id"
  }
}
```

### Environment Variables (Vercel Dashboard)

```
DATABASE_URL=postgresql://...
JWT_SECRET=...
JWT_REFRESH_SECRET=...
YOOKASSA_SHOP_ID=...
YOOKASSA_SECRET_KEY=...
TON_WALLET_ADDRESS=...
TRC20_WALLET_ADDRESS=...
TELEGRAM_BOT_TOKEN=...
FRONTEND_URL=https://bookly.vercel.app

✅ ЧЕКЛИСТ РЕАЛИЗАЦИИ
Phase 1: MVP (Core Features)

 Frontend setup (React + TypeScript + Tailwind + Vite)
 Backend setup (Express + Prisma + PostgreSQL)
 Database schema + migrations
 Auth: Email/Password регистрация и вход
 Auth: Telegram OAuth
 Главная страница: Book grid с фильтрами
 Book modal: Детали книги
 Favorites: Добавление/удаление
 Reader view: Базовый просмотр PDF
 Reading progress: Сохранение текущей страницы

Phase 2: Payment Integration

 ЮKassa: Интеграция + webhook
 USDT TON: Генерация адреса + верификация транзакции
 USDT TRC20: Генерация адреса + верификация
 Payment modal: UI для выбора способа оплаты
 Purchase history: Страница с историей покупок

Phase 3: Profile & Settings

 Profile page: Личный кабинет
 Avatar upload: Загрузка аватара
 Security: Смена пароля/email
 2FA: Google Authenticator integration
 Notification settings: Управление уведомлениями

Phase 4: Telegram Bot

 Bot setup: Основные команды
 Mini App: Интеграция с Telegram Web App
 Notifications: APScheduler для scheduled уведомлений
 Deep links: Переход на конкретные книги из бота

Phase 5: UI/UX Polish

 Bento design: Финальная стилизация
 Dark mode: Переключатель тем
 Animations: Framer Motion transitions
 Loading states: Skeletons и spinners
 Error handling: Toast notifications
 Mobile optimization: Touch gestures

Phase 6: Deployment

 Vercel: Деплой фронтенда
 Vercel: Деплой бэкенда (serverless functions)
 PostgreSQL: Production database (Supabase/Railway)
 Telegram Bot: Deployment (Heroku/Railway)
 Domain: Настройка custom domain
 SSL: HTTPS сертификаты


📊 МЕТРИКИ УСПЕХА
Performance:

Lighthouse Score > 90
FCP < 1.5s
LCP < 2.5s
TTI < 3.5s

Accessibility:

WCAG 2.1 AA compliance
Keyboard navigation
Screen reader support

SEO:

Meta tags
OpenGraph
Sitemap


Это детальный и структурированный план для реализации Telegram Mini App "Bookly". Каждый блок продуман с точки зрения UX, технической реализации и бизнес-логики. Готов ответить на любые вопросы по реализации! 🚀

🔍 Что такое Telegram Mini App?
Telegram Mini App = Web-приложение (React), которое:

✅ Открывается внутри Telegram (в нативном WebView)
✅ Использует Telegram Web App API (window.Telegram.WebApp)
✅ Имеет доступ к данным пользователя Telegram (ID, username, avatar)
✅ Интегрируется с Telegram UI (кнопки, haptic feedback, theme)
✅ Запускается через кнопку в боте или прямую ссылку


✅ Мой план полностью подходит для Telegram Mini App
В ТЗ выше я описал именно такую архитектуру:
1. Frontend (React Mini App)
Открывается ВНУТРИ Telegram через:
- Кнопку в боте: [Открыть библиотеку]
- Прямую ссылку: https://t.me/bookly_bot/app
2. Интеграция с Telegram Web App API
typescript// Инициализация Mini App
import { WebApp } from '@twa-dev/sdk';

useEffect(() => {
  // Telegram готов
  WebApp.ready();

  // Получаем данные пользователя
  const user = WebApp.initDataUnsafe.user;
  console.log(user.id, user.first_name, user.username);

  // Настройка UI
  WebApp.setHeaderColor('#8B7FF5');
  WebApp.setBackgroundColor('#F8F9FE');

  // Кнопка "Назад" в Telegram
  WebApp.BackButton.show();
  WebApp.BackButton.onClick(() => navigate(-1));
}, []);
3. Авторизация через Telegram
typescript// При первом запуске Mini App
const initData = WebApp.initData; // зашифрованные данные от Telegram

// Отправка на бэкенд для верификации
const response = await axios.post('/api/auth/telegram', {
  initData: initData
});

// Backend проверяет подлинность через hash
// и создает/логинит пользователя
```

---

## 🚀 Как это работает (Step-by-step)

### Шаг 1: Создание Telegram бота

```
1. BotFather → /newbot → название "Bookly"
2. Получаем токен: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
3. BotFather → /newapp → привязываем Mini App URL
   URL: https://bookly.vercel.app
```

### Шаг 2: Пользователь открывает бота

```
Пользователь: открывает @bookly_bot
Бот отправляет:
┌──────────────────────────────┐
│ 📚 Добро пожаловать в Bookly!│
│                              │
│ Читайте книги прямо в        │
│ Telegram без установки       │
│ дополнительных приложений    │
│                              │
│ [📖 Открыть библиотеку]      │ ← Кнопка с web_app
└──────────────────────────────┘
```

### Шаг 3: Mini App открывается внутри Telegram

```
Telegram Native WebView загружает:
https://bookly.vercel.app

React приложение:
1. Инициализирует Telegram Web App SDK
2. Получает telegram_id пользователя
3. Авторизует через backend
4. Показывает главную страницу с книгами
```

### Шаг 4: Все взаимодействие внутри Telegram

```
✅ Пользователь листает книги
✅ Добавляет в избранное
✅ Покупает книгу (платеж через Telegram Payments API)
✅ Читает книгу (PDF viewer внутри Mini App)
✅ Настраивает профиль

Все это БЕЗ ВЫХОДА из Telegram!

📱 Отличия от обычного Web App
Обычный Web AppTelegram Mini AppОткрывается в браузереОткрывается ВНУТРИ TelegramНет доступа к Telegram APIПолный доступ к WebApp APIТребует отдельную авторизациюАвтоматическая авторизация через TGНет интеграции с Telegram UIИспользует нативные элементы TG

🔧 Дополнения к ТЗ для Mini App
1. Telegram Web App SDK Integration
typescript// frontend/src/shared/lib/telegram.ts
import { WebApp } from '@twa-dev/sdk';

export const tg = WebApp;

export const initTelegramApp = () => {
  tg.ready();
  tg.expand(); // Развернуть на весь экран

  // Настройка темы
  if (tg.colorScheme === 'dark') {
    document.documentElement.classList.add('dark');
  }

  // Main button (для оплаты)
  tg.MainButton.setText('Оплатить');
  tg.MainButton.color = '#8B7FF5';

  return {
    user: tg.initDataUnsafe.user,
    theme: tg.colorScheme,
    platform: tg.platform,
  };
};
2. Нативные фичи Telegram
Haptic Feedback (вибрация):
typescript// При добавлении в избранное
WebApp.HapticFeedback.impactOccurred('light');

// При покупке книги
WebApp.HapticFeedback.notificationOccurred('success');

// При ошибке
WebApp.HapticFeedback.notificationOccurred('error');
Main Button (основная кнопка снизу):
typescript// При открытии модалки оплаты
WebApp.MainButton.setText('Оплатить 299₽');
WebApp.MainButton.show();
WebApp.MainButton.onClick(() => {
  processPayment();
});

// После оплаты
WebApp.MainButton.hide();
Back Button:
typescript// В reader view
WebApp.BackButton.show();
WebApp.BackButton.onClick(() => {
  closeReader();
});
Popup (нативные алерты Telegram):
typescriptWebApp.showPopup({
  title: 'Книга куплена!',
  message: 'Книга добавлена в вашу библиотеку',
  buttons: [
    { id: 'read', type: 'default', text: 'Читать' },
    { id: 'close', type: 'cancel', text: 'Закрыть' }
  ]
}, (buttonId) => {
  if (buttonId === 'read') {
    openReader();
  }
});
3. Telegram Payments (встроенная оплата)
typescript// Вместо внешних платежных систем используем Telegram Payments
WebApp.openInvoice('https://t.me/$invoice_link', (status) => {
  if (status === 'paid') {
    // Книга куплена
    addBookToLibrary();
  }
});
Backend генерирует invoice:
typescript// backend/src/services/telegram-payment.ts
import { Bot } from 'grammy';

const bot = new Bot(process.env.TELEGRAM_BOT_TOKEN!);

export const createInvoice = async (bookId: string, userId: string) => {
  const book = await prisma.book.findUnique({ where: { id: bookId } });

  const invoice = await bot.api.createInvoiceLink({
    title: book.title,
    description: `Автор: ${book.author}`,
    payload: JSON.stringify({ bookId, userId }),
    provider_token: '', // Пустой для Telegram Stars
    currency: 'XTR', // Telegram Stars
    prices: [{ label: book.title, amount: book.price * 100 }],
  });

  return invoice;
};
4. Deep Links (переход на конкретную книгу)
typescript// Бот отправляет уведомление с кнопкой
bot.api.sendMessage(userId, 'Новая книга в жанре Детектив!', {
  reply_markup: {
    inline_keyboard: [[
      {
        text: 'Открыть книгу',
        web_app: {
          url: 'https://bookly.vercel.app?book=123'
        }
      }
    ]]
  }
});

// Mini App обрабатывает параметр
const params = new URLSearchParams(window.location.search);
const bookId = params.get('book');
if (bookId) {
  openBookModal(bookId);
}
```

---

## 🔄 Обновленная схема работы

````
┌─────────────────────────────────────────────────┐
│              Telegram App                       │
│                                                 │
│  ┌───────────────────────────────────────────┐ │
│  │         @bookly_bot (Python)              │ │
│  │  /start → Кнопка "Открыть библиотеку"     │ │
│  │           (web_app button)                │ │
│  └───────────────────────────────────────────┘ │
│                      │                          │
│                      ▼                          │
│  ┌───────────────────────────────────────────┐ │
│  │      Telegram WebView (Mini App)          │ │
│  │  ┌─────────────────────────────────────┐  │ │
│  │  │   React App (bookly.vercel.app)     │  │ │
│  │  │                                     │  │ │
│  │  │  - Telegram Web App SDK             │  │ │
│  │  │  - Auto-auth через telegram_id      │  │ │
│  │  │  - Нативные кнопки TG               │  │ │
│  │  │  - Haptic feedback                  │  │ │
│  │  └─────────────────────────────────────┘  │ │
│  └───────────────────────────────────────────┘ │
│                      │                          │
│                      ▼                          │
│  ┌───────────────────────────────────────────┐ │
│  │       Backend API (Vercel/Railway)        │ │
│  │  - Верификация initData от Telegram       │ │
│  │  - JWT auth                               │ │
│  │  - PostgreSQL                             │ │
│  └───────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘

✅ Итого: Что у тебя будет

Пользователь открывает @bookly_bot → видит кнопку
Нажимает "Открыть библиотеку" → Mini App открывается ВНУТРИ Telegram
Автоматическая авторизация через Telegram ID (без логина/пароля)
Все действия внутри Telegram: листает книги, покупает, читает
Нативный UX: кнопки Telegram, вибрация, попапы
Уведомления от бота: "Новая книга!", "Дочитайте книгу" → кнопка открывает Mini App

Это полноценное приложение внутри Telegram, а не просто ссылка на сайт!
Мой план выше на 100% подходит для этого. Нужно только добавить интеграцию с @twa-dev/sdk и использовать Telegram Web App API. Все остальное (React, дизайн, логика) — идентично! 🚀

📚 BOOKLY — Telegram Mini App (Обновленное ТЗ)
🔧 TELEGRAM WEB APP INTEGRATION
1. Инициализация Mini App
typescript// frontend/src/shared/lib/telegram-app.ts

// Используем нативный Telegram Web App (без внешних библиотек)
declare global {
  interface Window {
    Telegram: {
      WebApp: TelegramWebApp;
    };
  }
}

interface TelegramWebApp {
  initData: string;
  initDataUnsafe: {
    query_id?: string;
    user?: TelegramUser;
    receiver?: TelegramUser;
    chat?: TelegramChat;
    chat_type?: string;
    chat_instance?: string;
    start_param?: string;
    can_send_after?: number;
    auth_date: number;
    hash: string;
  };
  version: string;
  platform: string;
  colorScheme: 'light' | 'dark';
  themeParams: ThemeParams;
  isExpanded: boolean;
  viewportHeight: number;
  viewportStableHeight: number;
  headerColor: string;
  backgroundColor: string;
  isClosingConfirmationEnabled: boolean;

  // Methods
  ready(): void;
  expand(): void;
  close(): void;
  MainButton: MainButton;
  BackButton: BackButton;
  SettingsButton: SettingsButton;
  HapticFeedback: HapticFeedback;
  CloudStorage: CloudStorage;
  BiometricManager: BiometricManager;

  // Events
  onEvent(eventType: string, callback: () => void): void;
  offEvent(eventType: string, callback: () => void): void;

  // Utils
  sendData(data: string): void;
  switchInlineQuery(query: string, choose_chat_types?: string[]): void;
  openLink(url: string, options?: { try_instant_view?: boolean }): void;
  openTelegramLink(url: string): void;
  openInvoice(url: string, callback?: (status: string) => void): void;
  showPopup(params: PopupParams, callback?: (button_id: string) => void): void;
  showAlert(message: string, callback?: () => void): void;
  showConfirm(message: string, callback?: (confirmed: boolean) => void): void;
  showScanQrPopup(params: ScanQrPopupParams, callback?: (text: string) => boolean): void;
  closeScanQrPopup(): void;
  readTextFromClipboard(callback?: (text: string) => void): void;
  requestWriteAccess(callback?: (granted: boolean) => void): void;
  requestContact(callback?: (granted: boolean) => void): void;

  // Invoice
  requestInvoice(params: InvoiceParams, callback?: (status: string) => void): void;
}

interface TelegramUser {
  id: number;
  is_bot?: boolean;
  first_name: string;
  last_name?: string;
  username?: string;
  language_code?: string;
  is_premium?: boolean;
  photo_url?: string;
}

interface ThemeParams {
  bg_color?: string;
  text_color?: string;
  hint_color?: string;
  link_color?: string;
  button_color?: string;
  button_text_color?: string;
  secondary_bg_color?: string;
  header_bg_color?: string;
  accent_text_color?: string;
  section_bg_color?: string;
  section_header_text_color?: string;
  subtitle_text_color?: string;
  destructive_text_color?: string;
}

// Экспорт инстанса
export const tg = window.Telegram?.WebApp;

// Инициализация приложения
export const initTelegramApp = () => {
  if (!tg) {
    console.error('Telegram Web App is not available');
    return null;
  }

  // Сообщаем Telegram, что приложение готово
  tg.ready();

  // Разворачиваем на весь экран
  tg.expand();

  // Включаем подтверждение закрытия
  tg.enableClosingConfirmation();

  // Настраиваем цвета под тему приложения
  tg.setHeaderColor('#8B7FF5');
  tg.setBackgroundColor(tg.colorScheme === 'dark' ? '#0F0F1E' : '#F8F9FE');

  // Применяем тему Telegram к приложению
  applyTelegramTheme(tg.themeParams);

  // Слушаем изменения темы
  tg.onEvent('themeChanged', () => {
    applyTelegramTheme(tg.themeParams);
  });

  // Слушаем изменения viewport (клавиатура)
  tg.onEvent('viewportChanged', () => {
    updateViewportHeight(tg.viewportHeight);
  });

  return {
    user: tg.initDataUnsafe.user,
    startParam: tg.initDataUnsafe.start_param,
    colorScheme: tg.colorScheme,
    platform: tg.platform,
    version: tg.version,
  };
};

// Применение Telegram темы к CSS переменным
const applyTelegramTheme = (theme: ThemeParams) => {
  const root = document.documentElement;

  if (theme.bg_color) root.style.setProperty('--tg-bg-color', theme.bg_color);
  if (theme.text_color) root.style.setProperty('--tg-text-color', theme.text_color);
  if (theme.button_color) root.style.setProperty('--tg-button-color', theme.button_color);
  if (theme.button_text_color) root.style.setProperty('--tg-button-text-color', theme.button_text_color);
  if (theme.link_color) root.style.setProperty('--tg-link-color', theme.link_color);

  // Переключаем класс темы
  if (tg.colorScheme === 'dark') {
    root.classList.add('dark');
  } else {
    root.classList.remove('dark');
  }
};

// Обновление высоты viewport (когда появляется клавиатура)
const updateViewportHeight = (height: number) => {
  document.documentElement.style.setProperty('--tg-viewport-height', `${height}px`);
};

🎯 ГЛАВНЫЕ КОМПОНЕНТЫ TELEGRAM WEB APP
2. Main Button (Основная кнопка)
typescript// frontend/src/shared/lib/telegram-main-button.ts

export class TelegramMainButton {
  private button = tg.MainButton;

  show(text: string, onClick: () => void) {
    this.button.setText(text);
    this.button.show();
    this.button.onClick(onClick);

    // Haptic feedback при клике
    tg.HapticFeedback.impactOccurred('medium');
  }

  hide() {
    this.button.hide();
    this.button.offClick();
  }

  showProgress() {
    this.button.showProgress();
  }

  hideProgress() {
    this.button.hideProgress();
  }

  setColor(color: string) {
    this.button.setParams({ color });
  }

  disable() {
    this.button.setParams({ is_active: false });
  }

  enable() {
    this.button.setParams({ is_active: true });
  }
}

export const mainButton = new TelegramMainButton();
Использование в компонентах:
typescript// features/payment/ui/PaymentModal.tsx

const PaymentModal = ({ book, onClose }) => {
  const [selectedMethod, setSelectedMethod] = useState('yookassa');

  useEffect(() => {
    // Показываем кнопку оплаты
    mainButton.show(`Оплатить ${book.price}₽`, handlePayment);

    return () => {
      // Скрываем при размонтировании
      mainButton.hide();
    };
  }, [book, selectedMethod]);

  const handlePayment = async () => {
    mainButton.showProgress();
    mainButton.disable();

    try {
      await processPayment(book.id, selectedMethod);
      tg.HapticFeedback.notificationOccurred('success');
      tg.showAlert('Книга успешно куплена!', () => {
        onClose();
        navigateToBook(book.id);
      });
    } catch (error) {
      tg.HapticFeedback.notificationOccurred('error');
      tg.showAlert('Ошибка оплаты. Попробуйте снова.');
      mainButton.enable();
    } finally {
      mainButton.hideProgress();
    }
  };

  return (
    <div className="payment-modal">
      {/* UI модалки без нижней кнопки - она уже в Telegram */}
    </div>
  );
};

3. Back Button
typescript// frontend/src/app/routing.tsx

import { useNavigate, useLocation } from 'react-router-dom';

export const TelegramBackButton = () => {
  const navigate = useNavigate();
  const location = useLocation();

  useEffect(() => {
    // Показываем кнопку "Назад" на всех страницах кроме главной
    if (location.pathname !== '/') {
      tg.BackButton.show();
      tg.BackButton.onClick(() => {
        tg.HapticFeedback.impactOccurred('light');
        navigate(-1);
      });
    } else {
      tg.BackButton.hide();
    }

    return () => {
      tg.BackButton.offClick();
    };
  }, [location, navigate]);

  return null;
};

// В App.tsx
<Router>
  <TelegramBackButton />
  <Routes>
    {/* ... */}
  </Routes>
</Router>

4. Haptic Feedback (Вибрация)
typescript// frontend/src/shared/lib/haptic.ts

export const haptic = {
  // Легкая вибрация (при навигации)
  light: () => {
    tg.HapticFeedback.impactOccurred('light');
  },

  // Средняя вибрация (при взаимодействии)
  medium: () => {
    tg.HapticFeedback.impactOccurred('medium');
  },

  // Сильная вибрация (при важных действиях)
  heavy: () => {
    tg.HapticFeedback.impactOccurred('heavy');
  },

  // Успешное действие
  success: () => {
    tg.HapticFeedback.notificationOccurred('success');
  },

  // Предупреждение
  warning: () => {
    tg.HapticFeedback.notificationOccurred('warning');
  },

  // Ошибка
  error: () => {
    tg.HapticFeedback.notificationOccurred('error');
  },

  // Выбор (при нажатии на элемент)
  selection: () => {
    tg.HapticFeedback.selectionChanged();
  },
};

// Использование
// При добавлении в избранное
const handleAddToFavorites = (bookId: string) => {
  haptic.light();
  addToFavorites(bookId);
};

// При покупке книги
const handlePurchase = async () => {
  try {
    await purchaseBook();
    haptic.success();
  } catch (error) {
    haptic.error();
  }
};

5. Cloud Storage (Сохранение данных)
typescript// frontend/src/shared/lib/telegram-storage.ts

export const telegramStorage = {
  // Сохранить данные
  async set(key: string, value: any): Promise<void> {
    return new Promise((resolve, reject) => {
      tg.CloudStorage.setItem(key, JSON.stringify(value), (error) => {
        if (error) reject(error);
        else resolve();
      });
    });
  },

  // Получить данные
  async get(key: string): Promise<any> {
    return new Promise((resolve, reject) => {
      tg.CloudStorage.getItem(key, (error, value) => {
        if (error) reject(error);
        else resolve(value ? JSON.parse(value) : null);
      });
    });
  },

  // Удалить данные
  async remove(key: string): Promise<void> {
    return new Promise((resolve, reject) => {
      tg.CloudStorage.removeItem(key, (error) => {
        if (error) reject(error);
        else resolve();
      });
    });
  },

  // Получить все ключи
  async getKeys(): Promise<string[]> {
    return new Promise((resolve, reject) => {
      tg.CloudStorage.getKeys((error, keys) => {
        if (error) reject(error);
        else resolve(keys || []);
      });
    });
  },
};

// Использование для сохранения настроек reader
const saveReaderSettings = async (settings: ReaderSettings) => {
  await telegramStorage.set('reader_settings', settings);
};

const loadReaderSettings = async (): Promise<ReaderSettings> => {
  const settings = await telegramStorage.get('reader_settings');
  return settings || defaultReaderSettings;
};

6. Popups и Alerts
typescript// frontend/src/shared/lib/telegram-dialogs.ts

export const telegramDialogs = {
  // Простой алерт
  alert: (message: string): Promise<void> => {
    return new Promise((resolve) => {
      tg.showAlert(message, resolve);
    });
  },

  // Подтверждение
  confirm: (message: string): Promise<boolean> => {
    return new Promise((resolve) => {
      tg.showConfirm(message, resolve);
    });
  },

  // Кастомный popup с кнопками
  popup: (params: {
    title: string;
    message: string;
    buttons: Array<{
      id: string;
      type: 'default' | 'ok' | 'close' | 'cancel' | 'destructive';
      text: string;
    }>;
  }): Promise<string> => {
    return new Promise((resolve) => {
      tg.showPopup(params, (buttonId) => {
        resolve(buttonId || '');
      });
    });
  },
};

// Использование
const handleDeleteBook = async (bookId: string) => {
  const confirmed = await telegramDialogs.confirm(
    'Вы уверены, что хотите удалить эту книгу из библиотеки?'
  );

  if (confirmed) {
    await deleteBook(bookId);
    haptic.success();
    await telegramDialogs.alert('Книга удалена');
  }
};

// Кастомный popup после покупки
const showPurchaseSuccess = async (bookTitle: string) => {
  const action = await telegramDialogs.popup({
    title: 'Книга куплена!',
    message: `"${bookTitle}" добавлена в вашу библиотеку`,
    buttons: [
      { id: 'read', type: 'default', text: 'Читать сейчас' },
      { id: 'library', type: 'default', text: 'В библиотеку' },
      { id: 'close', type: 'cancel', text: 'Закрыть' },
    ],
  });

  if (action === 'read') {
    navigateToReader(bookId);
  } else if (action === 'library') {
    navigateToLibrary();
  }
};

7. QR Scanner
typescript// frontend/src/features/qr-scanner/use-qr-scanner.ts

export const useQRScanner = () => {
  const scan = (): Promise<string | null> => {
    return new Promise((resolve) => {
      tg.showScanQrPopup(
        {
          text: 'Отсканируйте QR-код промокода',
        },
        (text) => {
          // Возвращаем true, чтобы закрыть сканер
          if (text) {
            tg.closeScanQrPopup();
            resolve(text);
            return true;
          }
          return false;
        }
      );
    });
  };

  return { scan };
};

// Использование для промокодов
const PromoCodeScanner = () => {
  const { scan } = useQRScanner();

  const handleScan = async () => {
    const code = await scan();
    if (code) {
      await applyPromoCode(code);
      haptic.success();
      await telegramDialogs.alert('Промокод применен!');
    }
  };

  return (
    <button onClick={handleScan}>
      Отсканировать промокод
    </button>
  );
};

8. Settings Button
typescript// frontend/src/widgets/settings-button/SettingsButton.tsx

export const TelegramSettingsButton = () => {
  const navigate = useNavigate();

  useEffect(() => {
    // Показываем кнопку настроек в хедере Telegram
    tg.SettingsButton.show();

    tg.SettingsButton.onClick(() => {
      haptic.light();
      navigate('/profile');
    });

    return () => {
      tg.SettingsButton.offClick();
      tg.SettingsButton.hide();
    };
  }, [navigate]);

  return null;
};

🔐 АВТОРИЗАЦИЯ ЧЕРЕЗ TELEGRAM
Backend: Валидация initData
typescript// backend/src/middleware/telegram-auth.ts

import crypto from 'crypto';

export const validateTelegramWebAppData = (initData: string): boolean => {
  const BOT_TOKEN = process.env.TELEGRAM_BOT_TOKEN!;

  // Парсим initData
  const urlParams = new URLSearchParams(initData);
  const hash = urlParams.get('hash');
  urlParams.delete('hash');

  // Сортируем параметры
  const dataCheckString = Array.from(urlParams.entries())
    .sort(([a], [b]) => a.localeCompare(b))
    .map(([key, value]) => `${key}=${value}`)
    .join('\n');

  // Создаем secret key
  const secretKey = crypto
    .createHmac('sha256', 'WebAppData')
    .update(BOT_TOKEN)
    .digest();

  // Вычисляем hash
  const calculatedHash = crypto
    .createHmac('sha256', secretKey)
    .update(dataCheckString)
    .digest('hex');

  // Сравниваем
  return calculatedHash === hash;
};

// Middleware для защиты роутов
export const telegramAuthMiddleware = async (req, res, next) => {
  const initData = req.headers['x-telegram-init-data'];

  if (!initData) {
    return res.status(401).json({ error: 'Unauthorized' });
  }

  if (!validateTelegramWebAppData(initData)) {
    return res.status(401).json({ error: 'Invalid Telegram data' });
  }

  // Извлекаем user из initData
  const urlParams = new URLSearchParams(initData);
  const userJson = urlParams.get('user');
  const user = userJson ? JSON.parse(userJson) : null;

  if (!user) {
    return res.status(401).json({ error: 'User not found' });
  }

  // Проверяем срок действия (auth_date)
  const authDate = parseInt(urlParams.get('auth_date') || '0');
  const currentTime = Math.floor(Date.now() / 1000);
  if (currentTime - authDate > 86400) {
    // 24 часа
    return res.status(401).json({ error: 'Auth data expired' });
  }

  // Добавляем пользователя в request
  req.telegramUser = user;
  next();
};
Frontend: Отправка initData
typescript// frontend/src/shared/api/client.ts

import axios from 'axios';
import { tg } from '../lib/telegram-app';

export const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_URL,
  headers: {
    'Content-Type': 'application/json',
  },
});

// Interceptor для добавления initData в каждый запрос
apiClient.interceptors.request.use((config) => {
  if (tg?.initData) {
    config.headers['X-Telegram-Init-Data'] = tg.initData;
  }
  return config;
});

// Interceptor для обработки ошибок
apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      // Unauthorized - показываем алерт
      tg?.showAlert('Сессия истекла. Перезапустите приложение.');
      tg?.close();
    }
    return Promise.reject(error);
  }
);

💳 TELEGRAM PAYMENTS (Встроенная оплата)
typescript// backend/src/routes/payment.routes.ts

import { Router } from 'express';
import { Bot } from 'grammy';

const router = Router();
const bot = new Bot(process.env.TELEGRAM_BOT_TOKEN!);

// Создание invoice для Telegram Stars
router.post('/create-invoice', telegramAuthMiddleware, async (req, res) => {
  const { bookId } = req.body;
  const userId = req.telegramUser.id;

  const book = await prisma.book.findUnique({ where: { id: bookId } });

  if (!book) {
    return res.status(404).json({ error: 'Book not found' });
  }

  // Создаем invoice link
  const invoiceLink = await bot.api.createInvoiceLink({
    title: book.title,
    description: `Автор: ${book.author}`,
    payload: JSON.stringify({ bookId, userId }),
    provider_token: '', // Пустой для Telegram Stars
    currency: 'XTR', // Telegram Stars
    prices: [
      {
        label: book.title,
        amount: Math.floor(book.price * 100), // В копейках
      },
    ],
    photo_url: book.coverUrl,
    photo_width: 600,
    photo_height: 900,
  });

  res.json({ invoiceLink });
});

// Webhook для обработки успешной оплаты
router.post('/telegram-payment-webhook', async (req, res) => {
  const update = req.body;

  if (update.pre_checkout_query) {
    // Pre-checkout query - подтверждаем заказ
    await bot.api.answerPreCheckoutQuery(
      update.pre_checkout_query.id,
      true
    );
  }

  if (update.message?.successful_payment) {
    // Оплата прошла успешно
    const payment = update.message.successful_payment;
    const payload = JSON.parse(payment.invoice_payload);

    // Добавляем книгу в purchases
    await prisma.purchase.create({
      data: {
        userId: payload.userId,
        bookId: payload.bookId,
        amount: payment.total_amount / 100,
        paymentMethod: 'telegram_stars',
        status: 'completed',
        transactionId: payment.telegram_payment_charge_id,
      },
    });

    // Отправляем уведомление пользователю
    await bot.api.sendMessage(
      update.message.from.id,
      `✅ Книга "${payload.bookTitle}" успешно куплена!\n\nОткройте приложение, чтобы начать чтение.`,
      {
        reply_markup: {
          inline_keyboard: [[
            {
              text: '📖 Открыть книгу',
              web_app: {
                url: `${process.env.MINI_APP_URL}?book=${payload.bookId}`,
              },
            },
          ]],
        },
      }
    );
  }

  res.sendStatus(200);
});

export default router;
Frontend: Открытие invoice
typescript// features/payment/model/use-telegram-payment.ts

export const useTelegramPayment = () => {
  const purchaseWithTelegram = async (bookId: string) => {
    try {
      // Получаем invoice link от backend
      const { data } = await apiClient.post('/payment/create-invoice', {
        bookId,
      });

      // Открываем форму оплаты в Telegram
      tg.openInvoice(data.invoiceLink, async (status) => {
        if (status === 'paid') {
          haptic.success();

          const action = await telegramDialogs.popup({
            title: 'Оплата успешна!',
            message: 'Книга добавлена в вашу библиотеку',
            buttons: [
              { id: 'read', type: 'default', text: 'Читать' },
              { id: 'close', type: 'ok', text: 'Закрыть' },
            ],
          });

          if (action === 'read') {
            navigateToReader(bookId);
          }
        } else if (status === 'cancelled') {
          haptic.warning();
        } else if (status === 'failed') {
          haptic.error();
          await telegramDialogs.alert('Ошибка оплаты. Попробуйте снова.');
        }
      });
    } catch (error) {
      haptic.error();
      await telegramDialogs.alert('Не удалось создать счет. Попробуйте позже.');
    }
  };

  return { purchaseWithTelegram };
};

🤖 TELEGRAM BOT (Обновленный)
python# telegram-bot/bot.py

from telegram import Update, WebAppInfo, InlineKeyboardButton, InlineKeyboardMarkup
from telegram.ext import Application, CommandHandler, MessageHandler, filters

MINI_APP_URL = "https://bookly.vercel.app"

async def start(update: Update, context):
    """Команда /start - показываем кнопку с Mini App"""
    keyboard = [
        [InlineKeyboardButton(
            "📖 Открыть библиотеку",
            web_app=WebAppInfo(url=MINI_APP_URL)
        )]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)

    await update.message.reply_text(
        "📚 <b>Добро пожаловать в Bookly!</b>\n\n"
        "Читайте тысячи книг прямо в Telegram.\n"
        "Без установки приложений, без рекламы.\n\n"
        "Нажмите кнопку ниже, чтобы начать:",
        parse_mode='HTML',
        reply_markup=reply_markup
    )

async def library_command(update: Update, context):
    """Команда /library - открывает Mini App на странице библиотеки"""
    keyboard = [
        [InlineKeyboardButton(
            "📚 Мои книги",
            web_app=WebAppInfo(url=f"{MINI_APP_URL}/my-books")
        )]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)

    await update.message.reply_text(
        "Ваша личная библиотека:",
        reply_markup=reply_markup
    )

async def send_unfinished_reminder(chat_id: int, book_title: str, progress: int, book_id: str):
    """Отправка напоминания о недочитанной книге"""
    keyboard = [
        [InlineKeyboardButton(
            "Продолжить чтение",
            web_app=WebAppInfo(url=f"{MINI_APP_URL}/reader/{book_id}")
        )]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)

    await bot.send_message(
        chat_id=chat_id,
        text=(
            f"📖 Вы остановились на <b>{progress}%</b> книги\n"
            f"<i>«{book_title}»</i>\n\n"
            f"Продолжим чтение?"
        ),
        parse_mode='HTML',
        reply_markup=reply_markup
    )

async def send_new_book_notification(chat_id: int, book: dict, genre: str):
    """Уведомление о новой книге в любимом жанре"""
    keyboard = [
        [InlineKeyboardButton(
            "Посмотреть книгу",
            web_app=WebAppInfo(url=f"{MINI_APP_URL}?book={book['id']}")
        )]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)

    price_text = "Бесплатно!" if book['isFree'] else f"Цена: {book['price']}₽"

    await bot.send_photo(
        chat_id=chat_id,
        photo=book['coverUrl'],
        caption=(
            f"🆕 <b>Новая книга в жанре {genre}!</b>\n\n"
            f"📕 {book['title']}\n"
            f"✍️ {book['author']}\n"
            f"💰 {price_text}"
        ),
        parse_mode='HTML',
        reply_markup=reply_markup
    )

if __name__ == '__main__':
    app = Application.builder().token(BOT_TOKEN).build()

    app.add_handler(CommandHandler('start', start))
    app.add_handler(CommandHandler('library', library_command))

    app.run_polling()

📱 ВАЖНЫЕ НЬЮАНСЫ TELEGRAM MINI APPS
1. CSS для Telegram WebView
css/* frontend/src/app/telegram.css */

:root {
  /* Используем Telegram theme colors */
  --bg-color: var(--tg-bg-color, #ffffff);
  --text-color: var(--tg-text-color, #000000);
  --button-color: var(--tg-button-color, #8B7FF5);
--button-text-color: var(--tg-button-text-color, #ffffff);
/* Viewport height с учетом клавиатуры */
--viewport-height: var(--tg-viewport-height, 100vh);
}
/* Безопасная зона для iOS */
body {
padding: env(safe-area-inset-top) env(safe-area-inset-right) env(safe-area-inset-bottom) env(safe-area-inset-left);
}
/* Убираем bounce эффект в iOS */
html, body {
overscroll-behavior: none;
-webkit-overflow-scrolling: touch;
}
/* Адаптация под Main Button (оставляем место снизу) */
.page-with-main-button {
padding-bottom: calc(env(safe-area-inset-bottom) + 60px);
}

### 2. Отключение нативного скролла (для custom reader)
```typescript
useEffect(() => {
  // Отключаем скролл страницы в Telegram
  document.body.style.overflow = 'hidden';
  document.documentElement.style.overflow = 'hidden';

  return () => {
    document.body.style.overflow = '';
    document.documentElement.style.overflow = '';
  };
}, []);
````

### 3. Deep Links и Start Parameters

```typescript
// При запуске Mini App с параметром
// https://t.me/bookly_bot/app?startapp=book_123

const { startParam } = initTelegramApp();

if (startParam) {
  // startParam = "book_123"
  const bookId = startParam.replace("book_", "");
  navigateToBook(bookId);
}
```

**Генерация ссылки в боте:**

```python
# Отправляем ссылку с параметром
share_link = f"https://t.me/bookly_bot/app?startapp=book_{book_id}"
await bot.send_message(
    chat_id,
    f"Поделиться книгой: {share_link}"
)
```

---

## ✅ ИТОГОВЫЙ ЧЕКЛИСТ ДЛЯ TELEGRAM MINI APP

- [ ] Инициализация `tg.ready()` и `tg.expand()`
- [ ] Применение Telegram theme colors
- [ ] Использование Main Button вместо custom кнопок
- [ ] Интеграция Back Button в навигацию
- [ ] Haptic Feedback на все действия
- [ ] Cloud Storage для настроек
- [ ] Telegram Popups вместо custom модалок
- [ ] QR Scanner для промокодов
- [ ] Settings Button в хедере
- [ ] Валидация initData на backend
- [ ] Telegram Payments для покупок
- [ ] Deep Links для переходов из уведомлений
- [ ] Адаптация CSS под Telegram WebView
- [ ] Тестирование в iOS и Android Telegram

---

Теперь у тебя **полностью интегрированное Telegram Mini App** с использованием всех нативных фич! 🚀
